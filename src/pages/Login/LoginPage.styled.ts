import styled from "styled-components";

export const LoginPageStyled = styled.div`
  form {
    display: grid;
    max-width: 620px;
    gap: 18px;
  }

  fieldset {
    display: grid;
    gap: 18px;
    margin: 0;
    padding: 0;
    border: 0;
  }
`;
