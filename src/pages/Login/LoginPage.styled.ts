import styled from "styled-components";

export const LoginFormStyled = styled.form`
  display: grid;
  max-width: 620px;
  gap: 18px;
`;

export const LoginFieldsStyled = styled.fieldset`
  display: grid;
  gap: 18px;
  margin: 0;
  padding: 0;
  border: 0;
`;
